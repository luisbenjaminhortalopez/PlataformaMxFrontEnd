export const AddButton = ({ onClick }) => (
    <button
      onClick={onClick}
      className="absolute top-6 right-6 w-10 h-10 rounded-full bg-black text-white text-2xl flex items-center justify-center shadow hover:scale-105 transition"
    >
      +
    </button>
  );
  